import { CreateDeviceLogDto } from './create-device-log.dto';

describe('CreateDeviceLogDto', () => {
  it('CreateDeviceLogDto가 정의되어야 한다', () => {
    expect(CreateDeviceLogDto).toBeDefined();
  });
});
